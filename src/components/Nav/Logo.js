import React from 'react';
import classes from './Style.css';

const Logo = (props) => (
        <img className={classes.Logo} src={'https://freeiconshop.com/wp-content/uploads/edd/burger-flat.png'}/>
);

    export default Logo;